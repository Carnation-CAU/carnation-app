# Carnation 앱 서버·실기기 원클릭 실행

## 실행 전 확인

1. 안드로이드 폰을 USB로 맥에 연결한다.
2. 폰 잠금을 해제한다.
3. 개발자 옵션의 **USB 디버깅**을 켠다.
4. 폰에 USB 디버깅 허용 창이 나오면 **허용**을 누른다.

## 처음 실행하거나 앱 코드가 변경됐을 때

터미널에 아래 명령어 한 줄을 그대로 붙여넣고 실행한다.

```bash
bash /Users/yarlang/carnation-app/start-phone.sh --test-alert
```

다음 작업이 자동으로 진행된다.

1. 로컬 서버 실행
2. 연결된 안드로이드 폰 확인
3. 최신 앱 빌드 및 설치
4. 기존 `adb reverse` 터널 제거 및 재생성
5. 폰에서 서버 접근 확인
6. 알림 권한 설정
7. 앱 실행 후 백그라운드 이동
8. 테스트 낙상 알림 1건 발송

## 다음부터 빠르게 실행할 때

앱을 다시 설치할 필요가 없다면 아래 명령어를 실행한다.

```bash
bash /Users/yarlang/carnation-app/start-phone.sh --skip-install --test-alert
```

## 연결만 하고 테스트 알림은 보내지 않을 때

```bash
bash /Users/yarlang/carnation-app/start-phone.sh --skip-install
```

## 언제 다시 실행해야 하나

다음 상황에서는 `--skip-install` 명령을 다시 실행한다.

- USB 케이블을 뺐다가 다시 연결했을 때
- `adb`가 재시작됐을 때
- 앱에 `서버에 연결되지 않았어요`가 나타날 때
- 테스트 이벤트를 보냈는데 앱 목록과 알림에 아무 반응이 없을 때

폰이 현재 진동 모드라면 알림 소리는 나지 않는다. 소리까지 확인하려면 폰을 소리 모드로 바꾼다.

