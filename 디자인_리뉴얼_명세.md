# carnation-app 디자인 리뉴얼 명세 (Claude Code용)

> 목표: **워크플로우·기능·로직은 그대로 두고, 화면 디자인만 처음부터 새로** 만든다.
> 레퍼런스: 토스(Toss) 스타일 — 깔끔한 미니멀, 넉넉한 여백, 큰 볼드 타이틀, 얇은 구분, 하단 풀와이드 버튼.
> 건드리지 말 것: `FallAlertViewModel`, `data/`, `model/`, `push/`, `remote/`, 내비 흐름·기능. **UI 레이어(테마 + 화면 컴포저블의 겉모습)만 교체.**

---

## 1. 색 팔레트 (Color.kt에 정의)

```kotlin
// 브랜드
val NavyDeep   = Color(0xFF0D1B3D)  // 헤드라인·강조 텍스트·다크 표면
val BluePrimary= Color(0xFF3A6CF4)  // 주요 버튼·활성 상태·링크·포인트
val BlueLight  = Color(0xFFA8C5FF)  // 연한 강조·선택 칩·보조 배경

// 뉴트럴 (토스풍)
val BgGray     = Color(0xFFF2F4F6)  // 화면 배경 / 그룹 카드 배경
val SurfaceWhite= Color(0xFFFFFFFF) // 카드·시트
val TextPrimary= Color(0xFF191F28)  // 본문 (거의 검정)
val TextSecondary=Color(0xFF6B7684) // 보조 회색
val TextMuted  = Color(0xFFA0A8B4)  // 힌트·캡션
val Hairline   = Color(0xFFE5E8EB)  // 얇은 구분선

// 상태 — 낙상은 안전 이슈라 빨강 유지 (브랜드 블루와 별개)
val DangerRed  = Color(0xFFE94A4A)  // 낙상 의심·위험
val DangerBg   = Color(0xFFFDECEC)  // 낙상 카드 배경
val WarnAmber  = Color(0xFFF5A623)  // 무동작 경계(선택)
val SuccessGreen=Color(0xFF16A34A)  // 정상 확인
```

색 사용 원칙:
- 기본 화면은 **뉴트럴(흰/회색) + 블루 포인트**. 파랑을 남발하지 말 것.
- **낙상 의심만 빨강**으로 확실히 구분(안전). 파랑은 브랜드·일반 액션용.
- 큰 면적에 진한 네이비/파랑 칠하지 않기. 포인트로만.

## 2. 타이포그래피 (토스풍: 크고 굵은 위계)

| 용도 | 크기 | 굵기 | 색 |
|---|---|---|---|
| 화면 큰 타이틀 | 24sp | Bold | NavyDeep/TextPrimary |
| 섹션 헤더 | 18sp | Bold | TextPrimary |
| 카드 제목 | 17sp | SemiBold | TextPrimary |
| 본문 | 16sp | Regular | TextPrimary |
| 보조/설명 | 14sp | Regular | TextSecondary |
| 캡션 | 13sp | Regular | TextMuted |

- 폰트는 시스템 기본(또는 Pretendard 있으면 그것). **굵기 2~3단계만** 써서 위계를 만든다.
- 줄간격 넉넉히(line-height 1.4~1.5).

## 3. 간격·모양 (여백이 미니멀의 핵심)

- 화면 좌우 패딩: **20dp**
- 카드 안쪽 패딩: **20dp**
- 요소 사이 세로 간격: 12~16dp, 섹션 사이 24~32dp
- 카드 모서리: **16dp** radius
- 버튼 모서리: **14dp** radius
- 그림자: **거의 안 씀.** 카드는 흰 배경 + 회색 배경(BgGray) 대비 또는 얇은 Hairline으로만 구분.

## 4. 핵심 컴포넌트 (토스 레퍼런스 그대로)

### 세그먼트 탭 (추천/계좌 스타일)
- 회색(BgGray) 둥근 컨테이너 안에, **활성 세그먼트만 흰색 + 아주 옅은 그림자**.
- 홈/확인한 알림 전환에 이 스타일 사용 가능(하단 탭 대신 상단 세그먼트도 OK — 아래 5장 참고).

### 리스트 행 (내 계좌 스타일)
- 왼쪽 **원형 아이콘**(색 배경 + 흰 심볼) + 제목(볼드) + 부제(회색).
- 행 사이 구분선 없이 **여백으로만** 분리. 탭 영역 넉넉히.

### 하단 풀와이드 버튼 (확인 버튼 스타일)
- 화면 하단 고정, 좌우 20dp 여백, **BluePrimary 배경 + 흰색 볼드 텍스트**, radius 14dp, 높이 56dp.
- 주요 액션(예: 확인) 1개는 이 스타일. 보조는 아웃라인.

### 큰 숫자·핵심 정보 (명세서 금액 스타일)
- 중요한 값(위험도, 무동작 시간)은 **크게 볼드**로. 라벨은 위에 작게 회색.

## 5. 화면별 적용 (기능·흐름 유지, 겉모습만)

### 홈 (AlertListScreen)
- 상단: 앱 타이틀(볼드) + 우측 ⚙ 설정 아이콘
- **무동작 카드**: 흰 카드에 "지금 3시간 12분째 움직임 없음" — 시간 숫자를 크게 볼드. 아이콘 하나. (평소=뉴트럴, 길어지면 WarnAmber 포인트)
- **낙상 알림 카드(최근 3개)**: DangerBg 배경 + DangerRed 텍스트·아이콘으로 확실히 눈에 띄게. 나머지 UI는 차분하니 이것만 강조됨.
- 하단: [부모님께 전화](아웃라인) / [119에 정보 보내기](아웃라인 or BluePrimary) 좌우
- 홈/확인한 알림 전환: 상단 세그먼트 탭 or 하단 탭 (기존 내비 유지, 겉모습만 토스풍)

### 확인한 알림 탭
- 회색 배경 위 흰 카드 리스트. 차분한 뉴트럴(빨강 X, 이미 확인된 것이므로). 각 행에 확인 결과(정상/도움) 작은 배지.

### 상세 (EventDetailScreen)
- 상단 큰 위험도 숫자(예: 90%)를 명세서 금액처럼 크게. 근거는 라벨-값 리스트(얇은 Hairline 구분). presence 제거 유지.
- 하단 [부모님께 전화] [119에 정보 보내기]

### 확인 완료 (ConfirmationDoneScreen)
- 가운데 큰 아이콘 + 한 줄 메시지(정상=SuccessGreen, 도움=DangerRed). 서버 상태 표시 없음. 하단 "목록으로" 버튼(토스풍 풀와이드).

### 설정 (SettingsScreen)
- 토스 입력 폼 스타일: 라벨(작은 회색) + 밑줄/얇은 박스 입력. 섹션으로 그룹.

## 6. AI티 없애는 체크리스트 (반드시)
- ❌ 기본 Material 보라(Purple) 테마 → ✅ 위 팔레트로 완전 교체
- ❌ 카드마다 진한 그림자 → ✅ 흰/회색 대비 + Hairline
- ❌ 균일하게 꽉 찬 레이아웃 → ✅ 넉넉한 여백, 요소 절제
- ❌ 색 여러 개 남발 → ✅ 뉴트럴 + 블루 포인트, 낙상만 빨강
- ❌ 작은 글씨 빽빽 → ✅ 큰 볼드 타이틀 + 명확한 3단 위계
- ✅ 하단 풀와이드 블루 버튼, 둥근 세그먼트 탭, 원형 아이콘 리스트 (토스 시그니처)

## 7. 작업 방식
- `ui/theme/`(Color, Type, Theme)부터 새 팔레트·타이포로 교체 → 그다음 화면별 컴포저블 겉모습 수정.
- **로직·상태·데이터·내비 흐름은 손대지 않는다.** 컴포저블의 Modifier·색·폰트·배치만 바꾼다.
- 화면 하나 바꿀 때마다 빌드·프리뷰 확인 후 다음으로.
