<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- API 33+ 에서 알림을 띄우려면 런타임 권한이 필요하다. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!--
    Android 11+ 패키지 가시성. 전화·문자 앱을 열려면 시스템이 해당 인텐트를
    해석할 수 있어야 한다. 발신 권한(CALL_PHONE / SEND_SMS)은 일부러 요청하지 않는다 —
    앱은 화면을 열어 줄 뿐이고 실제 발신은 사람이 한다.
    -->
    <queries>
        <intent>
            <action android:name="android.intent.action.DIAL" />
            <data android:scheme="tel" />
        </intent>
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data android:scheme="smsto" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CarnationFallAlert">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:theme="@style/Theme.CarnationFallAlert">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
        TODO(팀 확정 후): FCM 연동.
          <service android:name=".push.FallAlertMessagingService" android:exported="false">
              <intent-filter>
                  <action android:name="com.google.firebase.MESSAGING_EVENT" />
              </intent-filter>
          </service>
        자세한 절차는 app/src/main/java/com/carnation/fallalert/push/FCM_SETUP.md 참고.
        -->

    </application>

</manifest>
