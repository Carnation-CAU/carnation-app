<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 실제 색상은 Compose 의 CarnationTheme 이 담당한다. 여기는 시작 화면용 최소 설정. -->
    <style name="Theme.CarnationFallAlert" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/white</item>
        <item name="android:windowBackground">@color/window_background</item>
    </style>
</resources>
